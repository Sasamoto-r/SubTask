<h3 class="text-center">通知</h3>
    <%= button_to "全て削除", destroy_all_notifications_path, method: :delete %>
 
    <hr>
<% if @notifications.exists? %>
    <div class="users-index">
        <%= render @notifications %>
    </div>
<% else %>
    <p>通知はありません</p>
<% end %>